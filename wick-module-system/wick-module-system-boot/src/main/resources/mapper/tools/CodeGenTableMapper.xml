<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wick.boot.module.tools.mapper.ICodeGenTableMapper">

    <!-- 分页查询数据表信息 -->
    <select id="selectDataSourcePage" resultType="com.wick.boot.module.tools.model.entity.CodeGenTable">
        SELECT table_name, table_comment, create_time, update_time FROM information_schema.tables
        WHERE table_schema = (select database())
        AND table_name NOT LIKE 'system_codegen_%'
        AND table_name NOT IN (select table_name from system_code_gen_table)
        <if test="queryVO.name != null and queryVO.name != ''">
            AND lower(table_name) LIKE lower(concat('%', #{queryVO.name}, '%'))
        </if>
        <if test="queryVO.comment != null and queryVO.comment != ''">
            AND lower(table_comment) LIKE lower(concat('%', #{queryVO.comment}, '%'))
        </if>
        ORDER BY create_time DESC
    </select>


    <select id="selectByTableNames" resultType="com.wick.boot.module.tools.model.dto.CodeGenTableDTO">
        SELECT table_name, table_comment, create_time, update_time FROM information_schema.tables
        WHERE table_schema = (select database())
        AND table_name NOT LIKE 'system_codegen_%'
        <if test="tableNames != null and tableNames.size() > 0">
            AND `table_name` IN
            <foreach collection="tableNames" item="tableName" open="(" close=")" separator=",">
                #{tableName}
            </foreach>
        </if>
    </select>

</mapper>